// Import Flutter framework and required packages.
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import '../widgets/ui/glass.dart';

// Main welcome screen displayed when the app starts.
class WelcomeScreen extends StatelessWidget {

// Builds the UI for the welcome screen.
@override
Widget build(BuildContext context) {

// Main scaffold that provides the page structure.
return Scaffold(

// Hero section displaying the banner image and app statistics.
class _HeroVisual extends StatelessWidget {

// Builds the hero banner widget.
@override
Widget build(BuildContext context) {

// Highlighted word ("Meal") used in the hero heading.
class _HighlightWord extends StatelessWidget {

// Displays a small statistics pill (Meals, Donors, Partners).
class _StatPill extends StatelessWidget {

// Bottom "Get Started" button that navigates to the login page.
class _GetStartedBar extends StatelessWidget {

// Builds the Get Started button.
@override
Widget build(BuildContext context) {

// Card representing a user mode (Donor or Consumer).
class _ModeCard extends StatelessWidget {

// Builds the donor/consumer mode selection card.
@override
Widget build(BuildContext context) {

// Reusable image tile widget with optional click functionality.
class _ModeImageTile extends StatelessWidget {

// Builds the image tile.
@override
Widget build(BuildContext context) {